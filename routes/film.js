const pool = require("../config/config.js");
const route = require("./route.js");

// console.log(route);
// route query all film
route.get();

// route film with id
// route query film with category
